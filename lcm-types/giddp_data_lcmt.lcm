struct giddp_data_lcmt {
    float pos[3];
    float vWorld[3];
    float rpy[3];
    float omegaBody[3];
    float pfWorld[12];
    boolean contacts[4];
    int32_t N_mpcsteps;
    int32_t N_excuted;
    // float duration;
    // boolean trotInd;
}
